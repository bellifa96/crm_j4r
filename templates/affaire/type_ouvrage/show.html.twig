{% extends 'base.html.twig' %}

{% block title %}TypeOuvrage{% endblock %}

{% block body %}
    <h1>TypeOuvrage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_ouvrage.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ type_ouvrage.titre }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ type_ouvrage.couleur }}</td>
            </tr>
            <tr>
                <th>CouleurText</th>
                <td>{{ type_ouvrage.couleurText }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_affaire_type_ouvrage_index') }}">back to list</a>

    <a href="{{ path('app_affaire_type_ouvrage_edit', {'id': type_ouvrage.id}) }}">edit</a>

    {{ include('affaire/type_ouvrage/_delete_form.html.twig') }}
{% endblock %}
