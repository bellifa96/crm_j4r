<form method="post" action="{{ path('app_affaire_type_ouvrage_delete', {'id': type_ouvrage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_ouvrage.id) }}">
    <button type="submit" class="btn btn-danger mt-2">Supprimer</button>
</form>
