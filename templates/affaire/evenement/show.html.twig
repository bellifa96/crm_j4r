{% extends 'base.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
    <h1>Evenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ evenement.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ evenement.description }}</td>
            </tr>
            <tr>
                <th>DateDeDebut</th>
                <td>{{ evenement.dateDeDebut }}</td>
            </tr>
            <tr>
                <th>DateDeFin</th>
                <td>{{ evenement.dateDeFin }}</td>
            </tr>
            <tr>
                <th>Priorite</th>
                <td>{{ evenement.priorite }}</td>
            </tr>
            <tr>
                <th>TypeDEvenement</th>
                <td>{{ evenement.typeDEvenement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_affaire_evenement_index') }}">back to list</a>

    <a href="{{ path('app_affaire_evenement_edit', {'id': evenement.id}) }}">edit</a>

    {{ include('affaire/evenement/_delete_form.html.twig') }}
{% endblock %}
