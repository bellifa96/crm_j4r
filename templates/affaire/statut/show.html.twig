{% extends 'base.html.twig' %}

{% block title %}Statut{% endblock %}

{% block body %}
    <h1>Statut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ statut.titre }}</td>
            </tr>
            <tr>
                <th>Coleur</th>
                <td>{{ statut.couleur }}</td>
            </tr>
            <tr>
                <th>CouleurBG</th>
                <td>{{ statut.couleurBG }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ statut.destination ? statut.destination|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_affaire_statut_index') }}">back to list</a>

    <a href="{{ path('app_affaire_statut_edit', {'id': statut.id}) }}">edit</a>

    {{ include('affaire/statut/_delete_form.html.twig') }}
{% endblock %}
