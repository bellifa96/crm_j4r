<form method="post" action="{{ path('app_affaire_categorie_ouvrage_delete', {'id': categorie_ouvrage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_ouvrage.id) }}">
    <button class="btn">Delete</button>
</form>
