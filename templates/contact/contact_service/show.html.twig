{% extends 'base.html.twig' %}

{% block title %}ContactService{% endblock %}

{% block body %}
    <h1>ContactService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_service.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ contact_service.titre }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ contact_service.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contact_service_index') }}">back to list</a>

    <a href="{{ path('app_contact_service_edit', {'id': contact_service.id}) }}">edit</a>

    {{ include('contact/contact_service/_delete_form.html.twig') }}
{% endblock %}
