<form method="post" action="{{ path('app_contact_contact_service_delete', {'id': contact_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_service.id) }}">
    <button class="btn">Delete</button>
</form>
