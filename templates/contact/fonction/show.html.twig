{% extends 'base.html.twig' %}

{% block title %}Fonction{% endblock %}

{% block body %}
    <h1>Fonction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fonction.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ fonction.titre }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ fonction.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contact_fonction_index') }}">back to list</a>

    <a href="{{ path('app_contact_fonction_edit', {'id': fonction.id}) }}">edit</a>

    {{ include('contact/fonction/_delete_form.html.twig') }}
{% endblock %}
