{% extends 'base.html.twig' %}

{% block title %}Edit Contact{% endblock %}

{% block body %}
    <h1>Edit Contact</h1>

    {{ include('contact/contact/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_contact_contact_index') }}">back to list</a>

    {{ include('contact/contact/_delete_form.html.twig') }}
{% endblock %}
