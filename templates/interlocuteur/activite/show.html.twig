{% extends 'base.html.twig' %}

{% block title %}Activite{% endblock %}

{% block body %}
    <h1>Activite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ activite.titre }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ activite.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_interlocuteur_activite_index') }}">back to list</a>

    <a href="{{ path('app_interlocuteur_activite_edit', {'id': activite.id}) }}">edit</a>

    {{ include('interlocuteur/activite/_delete_form.html.twig') }}
{% endblock %}
