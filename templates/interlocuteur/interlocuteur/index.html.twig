{% extends 'base.html.twig' %}

{% block title %}Interlocuteur index{% endblock %}

{% block body %}
    <h1>Interlocuteur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Commentaire</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for interlocuteur in interlocuteurs %}
            <tr>
                <td>{{ interlocuteur.id }}</td>
                <td>{{ interlocuteur.type }}</td>
                <td>{{ interlocuteur.commentaire }}</td>
                <td>
                    <a href="{{ path('app_interlocuteur_interlocuteur_show', {'id': interlocuteur.id}) }}">show</a>
                    <a href="{{ path('app_interlocuteur_interlocuteur_edit', {'id': interlocuteur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_interlocuteur_interlocuteur_new') }}">Create new</a>
{% endblock %}
