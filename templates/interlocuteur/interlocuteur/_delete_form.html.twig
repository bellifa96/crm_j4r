<form method="post" action="{{ path('app_interlocuteur_interlocuteur_delete', {'id': interlocuteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interlocuteur.id) }}">
    <button class="btn">Delete</button>
</form>
