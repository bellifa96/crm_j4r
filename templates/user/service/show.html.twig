{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ service.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_service_index') }}">back to list</a>

    <a href="{{ path('app_user_service_edit', {'id': service.id}) }}">edit</a>

    {{ include('user/service/_delete_form.html.twig') }}
{% endblock %}
